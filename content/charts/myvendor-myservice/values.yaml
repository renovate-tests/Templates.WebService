app:
  name: myvendor-myservice

  image:
    registry: docker.axoom.cloud # Automatically replaced duing CI builds
    repository: services/myvendor-myservice
    tag: latest # Automatically replaced duing builds

  # resources:
  #   requests:
  #     memory: 256M
  #   limits:
  #     memory: 512M

  livenessProbe:
    initialDelaySeconds: 10 # Allow for long startup
    httpGet:
      port: ingress
      path: /health

  readinessProbe:
    initialDelaySeconds: 2 # Begin accepting traffic quickly
    httpGet:
      port: ingress
      path: /health
