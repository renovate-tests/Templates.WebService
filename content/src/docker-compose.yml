# Builds and runs the services themselves. External dependencies, exposed ports, etc. are configured in docker-compose.override.yml.
version: '3'

services:
  myservice:
    image: ${DOCKER_REGISTRY-docker-ci.axoom.cloud}/services/myservice:${VERSION-0.1-pre}
    build:
      context: ./Axoom.MyService
      dockerfile: Dockerfile
