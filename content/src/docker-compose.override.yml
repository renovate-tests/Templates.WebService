# Specifies additional dependencies and exposes ports for local development/testing.
version: '3'

services:
  myvendor-myservice:
    image: myvendor-myservice
    ports:
      - "12345:80"
    environment:
      ASPNETCORE_ENVIRONMENT: Development
